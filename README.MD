# nvim config

Personal Neovim configuration, written in Lua.

## Structure

```
init.lua              -- entry point, loads config modules
lua/config/options.lua -- editor options (tabstop, etc.)
```

## Status

Work in progress — a minimal starting point.

## Requirements

- Neovim >= 0.9
